# 📊 测试数据字段说明 & 异常分析说明

## 一、数据字段说明

### 1. sales_transactions.csv（销售交易表 - 10,000条）
这是主要的销售业务数据表。

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| **order_id** | String | 订单号（唯一标识） | `ORD2024021101251` |
| **date** | Date | 交易日期 | `2024-02-11` |
| **product_id** | String | 产品ID（外键，关联 product_catalog.csv） | `P0210` |
| **region** | String | 销售区域（华东/华南/华北/华中/西南/西北/东北） | `西南` |
| **channel** | String | 销售渠道（线上官网/线上第三方/线下直营店/线下经销商/企业团购） | `线下经销商` |
| **quantity** | Integer | 销售数量 | `1` |
| **unit_price** | Float | 单价（元） | `1135.9` |
| **discount_rate** | Float | 折扣率（0.0 = 无折扣，0.3 = 7折） | `0.0` |
| **is_returned** | String | 是否退款（是/否） | `否` |

---

### 2. product_catalog.csv（产品目录表 - 500条）
这是产品信息维度表，用于关联获取产品成本和分类信息。

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| **product_id** | String | 产品ID（主键） | `P0001` |
| **product_name** | String | 产品名称 | `耳机 标准版` |
| **category_l1** | String | 一级分类（电子产品/家居用品/食品饮料/服装鞋帽/运动户外等） | `电子产品` |
| **category_l2** | String | 二级分类（音频设备/厨房电器等） | `音频设备` |
| **cost_price** | Float | 成本价（元） - 用于计算毛利 | `374.16` |
| **launch_date** | Date | 上架日期 | `2022-05-23` |
| **status** | String | 产品状态（在售/缺货/下架） | `在售` |

---

### 3. regional_targets.csv（区域目标表 - 84条）
这是区域销售目标和实际完成情况表。

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| **region** | String | 区域（华东/华南/华北/华中/西南/西北/东北） | `华东` |
| **month** | String | 月份 | `2024-01` |
| **sales_target** | Float | 销售目标（元） | `150000` |
| **profit_target** | Float | 利润目标（元） | `60000` |
| **actual_sales** | Float | 实际销售额（元） | `145000` |
| **actual_profit** | Float | 实际利润（元） | `58000` |

---

## 二、如何分析「华东区线上渠道的毛利」

### 需要关联的表：
```
sales_transactions.csv  +  product_catalog.csv
      ↓ 通过 product_id 关联 ↓
```

### 分析步骤：
1. **筛选条件**：
   - region = '华东'
   - channel = '线上官网' 或 '线上第三方'

2. **关联字段**：
   ```
   sales_transactions.product_id  →  product_catalog.product_id
   ```

3. **计算公式**：
   ```
   单笔销售额 = quantity × unit_price × (1 - discount_rate)
   单笔成本 = quantity × cost_price  ←（来自 product_catalog）
   单笔毛利 = 单笔销售额 - 单笔成本
   毛利率 = (单笔毛利 / 单笔销售额) × 100%
   ```

4. **汇总分析**：
   ```
   华东区线上总毛利 = Σ(单笔毛利)  ← 对所有符合条件的订单求和
   华东区线上总销售额 = Σ(单笔销售额)
   华东区线上毛利率 = (华东区线上总毛利 / 华东区线上总销售额) × 100%
   ```

### 示例数据：
| order_id | product_id | quantity | unit_price | discount_rate | 销售额 | cost_price | 毛利 | 毛利率 |
|----------|-----------|----------|------------|---------------|--------|------------|------|--------|
| ORD001 | P0012 | 2 | ¥1000 | 0.10 | ¥1800 | ¥400 | ¥1000 | 55.6% |
| ORD002 | P0034 | 1 | ¥500 | 0.00 | ¥500 | ¥200 | ¥300 | 60.0% |

> **关键要点**：毛利计算**必须关联 product_catalog.csv**，因为只有那里有 cost_price（成本价）字段！

---

## 三、「检测到 60 个数据异常点」是什么意思？

### 什么是数据异常？
数据异常是指**与大多数数据的规律或预期值存在显著偏差**的数据点。

### 我们的系统检测的异常类型：

#### 1. **数值型异常**（极值检测）
- **检测方法**：基于统计的 Z-score 或 IQR（四分位距）
- **含义**：某些订单的数值（如数量、价格）远大于或远小于平均值
- **示例**：
  - 正常订单数量：1-5 件
  - 异常订单数量：32 件、48 件（突然大单）
  - 正常单价：100-5000 元
  - 异常单价：0.5 元、99999 元（可能是录入错误）

#### 2. **环比异常 (MoM - Month-over-Month)** ← 您看到的主要异常
- **检测方法**：相邻时间点的变化率分析
- **含义**：数据在短期内发生剧烈变化
- **计算公式**：
  ```
  环比变化率 = (当前值 - 前一值) / 前一值 × 100%
  ```

### 四、解读您看到的异常信息

```
📊 异常类型: 环比异常 (MoM)
⚡ 严重程度: HIGH (高)
📈 变化情况: 从 1 变为 32
📉 变化率: 3100.0%
💡 建议: undefined (系统正在生成优化建议)
```

**详细解读：**

#### 1. 「从 1 变为 32」是什么意思？
- **之前的值 = 1**：表示某个订单的销售数量为 1 件（这是正常水平，因为测试数据中大多数订单数量在 1-5 之间）
- **当前值 = 32**：表示某个订单的销售数量突增到 32 件
- **解读**：这是一个**数量激增**的数据点

#### 2. 「环比变化率 3100.0%」是什么意思？
```
变化率 = (32 - 1) ÷ 1 × 100% = 3100%
```
- **含义**：数量从 1 件增加到 32 件，**增长了 31 倍**！
- **正常范围**：通常环比变化在 -30% 到 +30% 之间
- **判断标准**：变化率超过 30%（系统设定的阈值）即被标记为异常

#### 3. 为什么会出现这样的异常？可能的业务原因：

| 可能性 | 说明 |
|--------|------|
| 🎉 **促销活动** | 618、双11等大促期间，客户大量采购 |
| 👥 **批量订单** | 企业团购或经销商进货，一次性大量采购 |
| 📦 **补货订单** | 某经销商库存告急，一次性大量补货 |
| 🎁 **赠品活动** | 某种买一送多的促销活动 |
| ⚠️ **数据录入错误** | 可能是操作员多输了一个 0（把 3 输成 32） |
| 🔄 **系统异常** | 订单系统批量处理时的计数错误 |

#### 4. 这类异常对业务分析的影响：
- **对平均值的影响**：一个 32 件的订单可能显著拉高平均值
- **对趋势分析的影响**：可能造成「销量暴涨」的假阳性信号
- **对毛利计算的影响**：如果单价正常，大额订单会产生更高的毛利（但也可能压低单价，因为批量通常有折扣）

---

## 五、在测试数据中，这些异常是怎么产生的？

在我们的测试数据生成脚本中，**有意设置了以下异常模式**：

```python
# 异常日期配置（在数据生成脚本中）
ANOMALY_DATES = {
    "2024-06-15": {"type": "销量突增", "multiplier": 5},  # 618大促
    "2024-11-11": {"type": "销量突增", "multiplier": 8},  # 双11大促
    "2024-08-05": {"type": "退款率激增", "return_rate": 0.5},  # 质量问题
}
```

**这意味着**：
- 6月15日（618大促）：销量是平时的 5 倍
- 11月11日（双11）：销量是平时的 8 倍
- 8月5日：退款率从 2-3% 骤增到 50%

**所以您看到的「从1变为32，变化率3100%」是**：
- 一条记录的 quantity（数量）字段在相邻记录间的剧烈变化
- 这是**大促期间正常的业务现象**，但从统计学角度看是异常点

---

## 六、如何处理这些异常？

### 在数据分析时：
1. **标记异常但不删除**：异常往往包含重要业务信息
2. **分析原因**：是业务事件（促销）？还是数据错误（录入）？
3. **分段分析**：将大促期间的数据单独分析，避免影响整体趋势判断

### 业务决策建议：
```
✅ 如果是真实的大促活动：
   ↳ 分析这些大促订单的毛利是否健康
   ↳ 检查库存是否充足
   ↳ 准备后续的大促策略优化

⚠️ 如果是录入错误：
   ↳ 复核原始订单
   ↳ 检查数据录入流程
   ↳ 设置更严格的输入验证规则
```

---

## 七、字段关联速查表

| 分析需求 | 主表 | 需关联的表 | 关联字段 | 计算字段 |
|----------|------|-----------|---------|---------|
| 按区域统计销售额 | sales_transactions | - | - | quantity × unit_price |
| 按产品分类统计 | sales_transactions | product_catalog | product_id | category_l1, category_l2 |
| 毛利分析 | sales_transactions | product_catalog | product_id | 销售额 - quantity × cost_price |
| 目标达成率分析 | sales_transactions | regional_targets | region + month | actual_sales / sales_target |
| 退款率分析 | sales_transactions | - | - | COUNT(is_returned='是') / COUNT(*) |

---

### 总结
1. **sales_transactions.csv** 包含 9 个字段，是核心业务数据
2. **分析「华东区线上渠道毛利」必须关联 product_catalog.csv** 来获取成本价
3. **60 个异常点**是系统检测到的偏离正常数据规律的记录，主要集中在销量的突发增长
4. **环比变化 3100%** 意味着数量从 1 件暴增到 32 件，增长了 31 倍（这在大促期间是正常业务现象，但从统计学角度看是异常）
5. 这些异常点**不是错误**，而是系统帮您发现的**值得关注的业务信号**！
